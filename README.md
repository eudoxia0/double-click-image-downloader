# double-click-image-downloader

This is a Manifest V3 extension to download images by double-clicking on them.

Building:

1. `make` produces a `.zip`
2. submit to mozilla
3. create a new release with the xpi
